  // 🔎 VALIDAÇÃO BACKEND (DESATIVADA TEMPORARIAMENTE)
  // fetch removido pois a rota /protegido não existe no backend atual
  // isso evitava bloqueio automático e erro de conexão

  mostrarMensagem("Sistema carregado 🚀");
