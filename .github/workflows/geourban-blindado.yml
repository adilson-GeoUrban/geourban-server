name: GeoUrban Auditoria 360

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  auditoria:
    runs-on: ubuntu-latest
    steps:
      - name: Baixar repositorio
        uses: actions/checkout@v4

      - name: Testar controlador
        run: echo "GEOURBAN AUDITORIA 360 - SOMENTE LEITURA"

      - name: Finalizar
        run: echo "AUDITORIA CONCLUIDA - NENHUMA ALTERACAO - NENHUM DEPLOY"
