rm -rf node_modules package-lock.json

npm install express cors path better-sqlite3

npm install

git add .
git commit -m "fix: full dependency rebuild for railway stable deploy"
git push origin main
